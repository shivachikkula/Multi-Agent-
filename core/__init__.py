"""Shared domain library for the multi-agent platform.

This package implements the "Agent Orchestration Layer" and everything it
depends on (agents, tools, memory, data, events, guardrails, security,
observability) from the reference architecture. It is imported by both the
``orchestrator`` and ``worker`` services so business logic lives in one
place.
"""
